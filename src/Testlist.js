import data from './data.json';
import Text from './Text.js';

export const Testlist = () => {

    
    // return data.map((current) => {
        // return current.params.map((cur) => {
            return(
                <div>
                    {/* <Text value={cur.Floor}/> */}
                    <Text/>
                </div>
            );
        // });
    // });
}
export default Testlist;